import 'package:flutter/material.dart';

const BorderRadius smallBorderRadius = BorderRadius.all(Radius.circular(11));
const BorderRadius mediumBorderRadius = BorderRadius.all(Radius.circular(20));
const BorderRadius largeBorderRadius = BorderRadius.all(Radius.circular(30));
const BorderRadius extralargeBorderRadius = BorderRadius.all(Radius.circular(90));
